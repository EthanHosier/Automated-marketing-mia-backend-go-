
  {
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Debug Tests",
        "type": "go",
        "request": "launch",
        "mode": "test",
        "program": "${workspaceFolder}/canva", // Adjust the path to your test file’s directory
        "args": ["-test.v"],
        "buildFlags": "-tags=integration", // Optional: specify any build tags if needed
        "env": {},
        "console": "integratedTerminal"
      }
    ]
  }
  
